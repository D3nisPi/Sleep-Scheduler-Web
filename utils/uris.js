import { HOST, PORT } from "/utils/config.js";

export const loginUri = `${HOST}:${PORT}/auth/login/`;
export const refreshUri = `${HOST}:${PORT}/auth/refresh/`;
export const usersUri = `${HOST}:${PORT}/users/`;
export const notesUri = `${HOST}:${PORT}/notes/`;
export const goalsUri = `${HOST}:${PORT}/goals/`;
